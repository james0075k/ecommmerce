/**
 * shadcn/ui components import `cn` from here by convention (components.json
 * aliases.utils). Re-exported from @bazaar/ui so the storefront, the admin
 * panel and any future app share one implementation.
 */
export { cn } from '@bazaar/ui';
