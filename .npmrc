# Prisma lives at the repo root (prisma/schema.prisma) but @prisma/client must be
# resolvable from apps/api. Hoisting keeps a single generated client for the whole
# workspace instead of one per package. See README > "Why shamefully-hoist".
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
